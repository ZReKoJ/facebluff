<!DOCTYPE html>
<html lang="es">

<head>
    <title>Facebluff</title>
    <meta charset="utf-8" />
    <link rel="icon" href="/resources/icon/logo.png" />
    <link rel="stylesheet" href="/css/utils.css" />
    <link rel="stylesheet" href="/css/modify-profile.css" />
</head>

<body>
    <div class="container">
        <%- include("header") %>
        <div class="main">
            <div class="modify-profile">
                <h2>Modificar perfil</h2>
                <form action="/profile/modify_profile" method="post" enctype="multipart/form-data">
                    <input type="text" name="username" placeholder="Nombre de usuario" value=<%=user.username%>>
                    <input type="email" name="email" placeholder="Email" value=<%=user.email%>>
                    <input type="password" name="password" placeholder="Contraseña" value=<%=user.password%>>
                    <input type="password" name="password" placeholder="Repita tu contraseña" value=<%=user.password%>>
                    <input type="date" name="birthdate" placeholder="Fecha de nacimiento" 
                    value=<%=new Date(user.birthdate).toLocaleDateString(undefined, {year:"numeric",month:"2-digit", day:"2-digit"})%>>
                    <div class="gender">
                        <input type="radio" name="gender" value="male" <%if(user.gender==="male" ){%> checked
                        <%}%> > Masculino<br>
                        <input type="radio" name="gender" value="female" <%if(user.gender==="female" ){%> checked
                        <%}%>> Femenino<br>
                        <input type="radio" name="gender" value="other" <%if(user.gender==="other" ){%> checked
                        <%}%>> Otro
                    </div>
                    <input type="file" name="avatar" id="inputfile" />
                    <textarea name="description" placeholder="Descripción" rows="10" cols="30"><%=user.description%></textarea>
                    <input type="submit" value="Modificar" class="modify button">
                </form>
            </div>
        </div>
    </div>
    <%- include("footer") %>
</body>

</html>